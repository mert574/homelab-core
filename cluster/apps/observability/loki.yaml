# Loki (single binary): receives logs over its native OTLP endpoint (the collector
# forwards them), filesystem storage on a PVC. Values vendored from pulse-pager
# deploy/observability/loki/values.yaml (PVC trimmed for the node disk).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pulse-loki # fullnameOverride pins the service to pulse-loki regardless
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1" # alongside Tempo, before the collector
spec:
  project: default
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: loki
    targetRevision: 6.24.0
    helm:
      valuesObject:
        fullnameOverride: pulse-loki
        deploymentMode: SingleBinary
        singleBinary:
          replicas: 1
          persistence:
            enabled: true
            size: 4Gi
        # SingleBinary mode: zero the SimpleScalable targets, which the chart still
        # defaults to >0 (the validate template rejects both being non-zero).
        read:
          replicas: 0
        write:
          replicas: 0
        backend:
          replicas: 0
        chunksCache:
          enabled: false
        resultsCache:
          enabled: false
        loki:
          auth_enabled: false
          commonConfig:
            replication_factor: 1
          schemaConfig:
            configs:
              - from: "2024-01-01"
                store: tsdb
                object_store: filesystem
                schema: v13
                index:
                  prefix: index_
                  period: 24h
          storage:
            type: filesystem
          limits_config:
            allow_structured_metadata: true
            volume_enabled: true
        gateway:
          enabled: false
        lokiCanary:
          enabled: false
        test:
          enabled: false
  destination:
    server: https://kubernetes.default.svc
    namespace: pulse-system
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions: [CreateNamespace=true, ServerSideApply=true]
